<nav class="navbar">
  <div>
    <a href="/">Home</a>
    <a href="/about">About</a>
    <% if (user) { %>
      <a href="/profile">Profile</a>
    <% } %>
  </div>
  <div>
    <% if (user) { %>
      <img src="<%= user.photos[0].value %>" class="profile-pic">
      <a href="/auth/google?prompt=select_account">Switch Account</a>
      <a href="/logout">Logout</a>
    <% } else { %>
      <a href="/auth/google?prompt=select_account">Login</a>
    <% } %>
  </div>
</nav>